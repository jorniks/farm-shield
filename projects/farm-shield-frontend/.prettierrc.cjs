// module.exports = {
//   singleQuote: true, // Use single quotes instead of double quotes
//   jsxSingleQuote: true, // Use single quotes in JSX
//   semi: true, // Add semicolons at the ends of statements
//   tabWidth: 2, // Set the number of spaces per indentation level
//   trailingComma: 'es5', // Only add trailing commas where valid in ES5 (objects, arrays, etc.)
//   printWidth: 80, // Wrap lines at 80 characters
//   endOfLine: 'auto', // Maintain existing line endings (mixed values within one file are normal)
//   arrowParens: 'avoid', // Omit parens when possible (e.g., x => x)
// }
